{
  "name": "hk",
  "description": "hk personal cross-project commands. /hk:orca:orchestrate — coordinate parallel Orca sub-worktree workers (provision → gather → brief → dispatch → cross-model review → fix loop → integration landing); command mechanics delegated to the orca-cli & orchestration skills.",
  "version": "0.1.0"
}

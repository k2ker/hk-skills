## 기록 컨벤션 (v2)

이 프로젝트는 조각 로그 컨벤션을 쓴다 — 일단락은 `docs/log/YYYY-MM-DD-주제.md` 조각으로(순번 금지·박제), 현재 상태는 `MEMORY.md`(책갈피 — 열린 것만 3줄), 결정은 `docs/DECISIONS.md`(3줄 상한). 병렬 워커는 기록 금지(보고만, 리드가 랜딩 때 기록). 규칙 정본은 hk 플러그인 `record` 스킬 하나다 (`docs/log/README.md`는 프로젝트 로컬 요약이자 opt-in 표식).
